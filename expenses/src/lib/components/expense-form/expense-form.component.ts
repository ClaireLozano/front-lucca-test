import { Component } from '@angular/core';

@Component({
	selector: 'exp-expense-form',
	template: ``,
	styleUrls: ['./expense-form.component.css'],
})
export class ExpenseFormComponent {
	// formulaire avec un input de type qui contien l'expense pour l'edition
}
